// jsconfig.json
{
    "compilerOptions": {
      "baseUrl": ".",
      "paths": {
        "@/*": ["./react-src/*"] // Mapea @/ a react-src/
      }
    },
    "exclude": ["node_modules", "dist"] // Excluye carpetas
  }